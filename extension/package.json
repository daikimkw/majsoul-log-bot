{
  "name": "@majsoul-log-bot/extension",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "zip": "wxt zip",
    "compile": "wxt prepare && tsc --noEmit",
    "postinstall": "wxt prepare"
  },
  "dependencies": {
    "protobufjs": "^7"
  },
  "devDependencies": {
    "typescript": "^5",
    "wxt": "^0.20"
  }
}
